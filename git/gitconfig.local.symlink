[filter "lfs"]
        clean = git-lfs clean -- %f
        smudge = git-lfs smudge -- %f
        process = git-lfs filter-process
        required = true
[user]
        name = Hans Gerwitz
        email = hans@gerwitz.com
[credential]
        helper = osxkeychain
